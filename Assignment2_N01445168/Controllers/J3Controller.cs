using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Web.Http;

namespace Assignment2_N01445168.Controllers
{
    public class J3Controller : ApiController
    {
        //question - https://cemc.math.uwaterloo.ca/contests/computing/2014/stage%201/juniorEn.pdf
        // indicate how many points each of the players start off with
        // int david = 100
        // int antonia = 100
        //input the number of rounds. Input should be >= 1 OR <=15
        //input roll of antonia and roll of David for that round with a space inbetween the two integers
        // each roll should be >=1 OR <=6
        // if antonia's roll is > davids, then davids total points - antonia's roll
        // if david's roll is > antonia's, then antonias total points - davids roll
        //if antonia's roll == davids roll, then do nothing

    }
}
